问题一：为什么 20000 条数据是 10000 个 iteration？

因为 micro-batch size 是 2：

20000/2=10000
问题二：设置梯度累积为 8，为什么日志还是 10000 步？

日志中的 step 通常记录每个 micro-batch，而不是每次参数更新。每 8 个 micro-batch 累积后才执行一次 optimizer 更新。

有效 batch size 为：

2×8=16
问题三：loss 为什么上下波动，而不是一直下降？

每个 batch 的样本不同，文本难度和有效 token 数不同，所以单步 loss 有噪声。判断训练趋势要看滑动平均或较长区间，而不是要求每一步单调下降。

问题四：为什么 aux loss 是 0？

因为使用的是普通 Dense 模型，没有启用 MoE，所以没有专家负载均衡等辅助损失。

问题五：训练结束 loss 约 4，代表准确率是多少？

不能直接把交叉熵 loss 当作准确率。它描述模型给真实下一个 token 分配概率的情况。

可以粗略计算困惑度：

PPL=e
L

若：

L=4

则：

PPL≈e
4
≈54.6

可以直观理解为：模型在当前数据上平均仍面临较大的下一 token 不确定性。但这个数字受 tokenizer、数据和计算方式影响，不能脱离实验设置横向乱比。

面试中这块可能怎么问
问：Dataset 和 DataLoader 有什么区别？

可以答：

Dataset 定义单条样本如何读取和预处理，通常实现 __len__ 和 __getitem__；DataLoader 负责按 batch 迭代 Dataset，并提供 shuffle、多进程加载和 batch 拼接等能力。

问：为什么要设置 max_seq_len？

Transformer 的计算和显存开销随序列长度显著增加。训练时需要将过长文本截断，将较短文本 padding 到统一长度，才能组成 batch。

问：为什么不是直接把字符串送入模型？

神经网络只能处理数值张量，所以字符串必须先由 tokenizer 转成 token ID，再经过 embedding 层映射成连续向量。
七、关于这段代码，你现在应能回答的面试问题
1. Dataset 中为什么实现 __len__ 和 __getitem__？

__len__ 返回数据集样本数量；__getitem__ 定义如何读取和处理第 i 条样本。

2. 为什么 max_length - 2？

给 BOS 和 EOS 两个特殊 token 各预留一个位置。

3. 为什么需要 padding？

batch 中不同文本长度不同，必须补齐到统一长度，才能堆叠成规则张量。

4. 为什么 labels 中 PAD 被设成 -100？

让交叉熵损失忽略 padding 位置，避免无意义的 PAD token 参与训练。

5. 为什么 labels = input_ids.clone()？

自回归语言模型用文本本身作为监督目标。模型内部通常会将 logits 和 labels 错位一位，执行 next-token prediction。

6. 为什么 token ID 使用 torch.long？

embedding 层需要整数索引，PyTorch 通常要求索引张量为 LongTensor。

信号，模型计算 loss 时会将 logits 和 labels 错位一个 token。

DataLoader 输出的形状是什么？

若 batch_size=2、max_length=32：

[2,32]
max_length 增大会发生什么？
能保留更多上下文；
attention 计算量增大；
激活显存增加；
训练速度下降。

标准 self-attention 的计算量对序列长度近似为：

O(L
2
)
为什么 PAD 对应 labels 要设为 -100？

使交叉熵忽略 PAD 位置，不让模型学习预测无意义的填充符。
